import java.util.HashMap;

public class State {
    public enum states {
        MAIN_MENU,
        EXIT(0),
        ADD(1),
        REMOVE(2),
        UPDATE(3)
        DISPLAY(4),
        LOOKUP(5),
    }
}
